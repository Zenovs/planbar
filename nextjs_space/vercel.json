{
  "version": 2,
  "buildCommand": "sed -i '/output.*=.*\"\\/home/d' prisma/schema.prisma && prisma generate && (prisma db push --accept-data-loss || true) && next build",
  "crons": [
    {
      "path": "/api/cron/moco-sync",
      "schedule": "0 4 * * *"
    }
  ]
}
